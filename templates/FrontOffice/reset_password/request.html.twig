{% extends 'FrontOffice/body.html.twig' %}

{% block title %}Reset your password{% endblock %}
{% block breadcrumb %}
    <span></span>
{% endblock %}
{% block pagetitle %}<span></span>{% endblock %}
{% block content %}
{#    {% for flashError in app.flashes('reset_password_error') %}#}
{#        <div class="alert alert-danger" role="alert">{{ flashError }}</div>#}
{#    {% endfor %}#}
{#    <h1>Reset your password</h1>#}

{#    {{ form_start(requestForm) }}#}
{#        {{ form_row(requestForm.email) }}#}
{#        <div>#}
{#            <small>#}
{#                Enter your email address and we we will send you a#}
{#                link to reset your password.#}
{#            </small>#}
{#        </div>#}

{#        <button class="btn btn-primary">Send password reset email</button>#}
{#    {{ form_end(requestForm) }}#}
    <section class="padding-y-100 bg-light">
        <div class="container">
            <div class="row">
                <div class="col-lg-6 mx-auto">
                    <div class="card shadow-v2">
                        {% for flashError in app.flashes('reset_password_error') %}
                            <div class="alert alert-danger" role="alert">{{ flashError }}</div>
                        {% endfor %}
                        <div class="card-header px-lg-5 border-bottom">
                            <h4 class="mt-4">Get Your Password</h4>
                        </div>
                        <div class="card-body">
                            {{ form_start(requestForm) }}
                            <form action="#" method="POST" class="px-lg-5">
                                <p class="marginBottom-40">
                                    Lost your password? Please enter your email address. You
                                    will receive a link to create a new password via email.
                                </p>
                                <div class="input-group input-group--focus marginBottom-40">
                                    <div class="input-group-prepend">
                                        <span class="input-group-text bg-white ti-email"></span>
                                    </div>
{#                                    <input#}
{#                                            type="email"#}
{#                                            class="form-control border-left-0 pl-0"#}
{#                                            placeholder="Email"#}
{#                                            required#}
{#                                    />#}
                                    {{ form_widget(requestForm.email,{attr: {class: 'form-control border-left-0 pl-0','placeholder':'Email'}}) }}
                                </div>
                                <button class="btn btn-block btn-primary mb-5">
                                    Reset Password
                                </button>
                                {{ form_end(requestForm) }}
                            </form>
                        </div>
                    </div>
                </div>
            </div>
            <!-- END row-->
        </div>
        <!-- END container-->
    </section>
{% endblock %}
